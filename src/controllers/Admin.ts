import { RequestHandler } from "express";
import Admin from "../models/Admin";

export const authAdmin: RequestHandler = (req, res) => {};
export const createAdmin: RequestHandler = (req, res) => {};
